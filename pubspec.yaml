name: sales_micros_services
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  hexcolor: ^2.0.4
  font_awesome_flutter: ^9.1.0
  otp_text_field: ^1.1.1  
  http:

dev_dependencies:
  flutter_test:
    sdk: flutter
  test:
  mockito:
  faker:


flutter:
  uses-material-design: true
  assets:
    - lib/ui/assets/

